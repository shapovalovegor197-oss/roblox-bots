# Состояние и задачи — 31.08.2026, полдень

Правило этого файла: **сделанным считается только то, что отработало в логе с
числом**. Написанный код — гипотеза, пока его не видели в деле. Ночь 30–31.08
показала, почему это важно: половина «готового» разваливалась на первом же
живом круге.

---

## Проверено числами

* **Лок базы** — сотни повторов, 28–48 секунд на цикл, держится 80 (после
  перерождения 90). Фазы: разворот 6.5, наведение 1.9, подход 2.9, дошаг 0.7.
* **Чтение наличных** — стенд `bench_offline.py`: 14 кадров из 14 верно, ноль
  вранья. Требуется знак `$`, суффикс K/M/B и мантисса меньше 1000.
* **Крестик закрытия окна** — 2 из 2 настоящих окна находятся, ложных 4 на 156
  чистых кадрах. Проверяется форма Х: точки у диагоналей и обязательно в центре.
* **Дорога к ленте** — признак «синее полотно под ногами»: на ленте 0.98, на
  траве 0.00, на своей дорожке 0.69. В логе «на ленте на 3-м шаге».
* **Чтение карточек ленты** — за прогон опознано 12 разных брейнротов.
* **Покупка целей ребёрна** — обе куплены живьём (Trulimero Trulicina,
  Chimpanzini Bananini), каждая по промпту с удержанием E.
* **Сбор денег** — ОДИН замер: 27 400 000 -> 37 910 000 за проход по левому
  ряду, ровно сумма из подписи `Collect $10.9M`.
* **Самозамер поворота** — 0.102 / 0.070 / 0.189 град на единицу в разных
  условиях; мера снимается на старте по сдвигу сцены, без движения камеры по
  вертикали.

## Написано, но в деле НЕ ВИДЕЛ

* **Продажа слабых** (`Farmer.sell_below`). Проход с продажей отработал один
  раз и не продал никого; строка «продаю» не печаталась ни разу.
  **Проверка:** строка `продаю <имя> (доход N < планки M)` и рост кэша.
* **Ребёрн ботом.** Ноль. Единственное перерождение сделал пользователь руками.
  **Проверка:** `ребёрнов = 1` в `var/farm_status.json`.
* **Подтверждение ребёрна по обнулению денег.** Написано после того, как
  выяснилось, что лидерборд читается не всегда. Не срабатывало.
  **Проверка:** строка `ребёрн подтверждён обнулением денег`.
* **Инструкция для второй машины** (`НОУТБУК.md`). Текст, по которому никто не
  проходил. **Проверка:** ферма поднялась на ноуте по этим шагам без правок.

## Не решено вовсе

* **Воровство.** Цели уносят с базы: 07:09 в панели Chimpanzini горит, а
  Trulimero, купленный часом раньше, снова тёмный. Держать обе одновременно
  пока не удавалось. Сокращённое стояние у ленты (35 с) — смягчение, не решение.
* **Сумма требования не читается** — `нужно $None`. Строку прогресса
  `$ X / $ Y` OCR не разбирает, денежный порог ребёрна фактически выключен.
* **Сбор даёт прирост не каждый круг** — в дневном прогоне ни одного
  «собрано рядом». Работает не стабильно, а от случая.
* **Сбоев много** — 50 на 68 кругов в ночном отрезке.
* **Смена освещения ломает цветные детекторы.** За ночь чинил дважды (пад
  сверху, крестик). Вернётся при следующей смене оформления.

---

## Ближайшие три задачи

Каждая закрывается наблюдением, а не правкой:

1. **Увидеть продажу.** Строка `продаю <имя>` с числами и рост кэша после неё.
   Если не появляется — понять, читается ли промпт `F Sell` вообще (снять кадр
   стоя вплотную к брейнроту).
2. **Довести ребёрн ботом** до `ребёрнов = 1`. Сейчас цель одна —
   `Glorbo Fruttodrillo`; денег хватает всегда, дело только в том, чтобы цель
   выпала на ленте и не была украдена до нажатия.
3. **Получить `собрано рядом` три круга подряд.** Пока это единичный замер, а
   не работающий узел.

## Дальше, когда эти три закрыты

* починить чтение суммы требования (`$ X / $ Y`);
* защита от воровства: держать базу запертой плотнее, уходить к ленте только с
  запертой базой и возвращаться до истечения таймера;
* склад на второй машине (`sklad-1`, `scenarios/steal.py`) — передача ценного
  перед перерождением. В коде заготовка, живьём не проверялась ни разу.

---

## Ночь 03.09: попытка ускорить дорогу — что вышло

**Замер по фазам** (дорога к ленте 38.6 с): респавн 13.1, пеленг 6.5,
разворот 14.8, шаги 4.0. Внутри «респавна»: сам respawn 5.5 (меню 2.9,
подтверждение 2.0, анимация 0.7), приведение камеры 7.3, таблица игроков 0.4.

**Три попытки резать — три отката.** Все три раза я неверно назвал виновника:

1. остаток разворота одной протяжкой на 225 вместо семи по 45 (угол тот же) —
   откатил, увидев «по синеве»;
2. паузы камеры 0.4 -> 0.15 — откатил по тому же признаку;
3. пауза после респавна 1.2 -> 0.4, срезанная ЗАОДНО с разбивкой на замеры, —
   вот эту я и не заметил, пока откатывал первые две.

**А признак был ложный.** «Доступил на ленту по синеве» — не поломка: на кадре
03:24:10 бот стоит на ленте, промпт `Purchase Gangster Footera $4K` в кадре,
карточки читаются. Просто `on_belt` требует, чтобы низ кадра был синим на 0.8, а
бот встаёт у КРАЯ полотна, и в полосу попадает трава. Прямой выход «на 3-м шаге»
и выход «по синеве» приводят в одно место, второй просто длиннее.

**Что осталось из ускорения:** только распознавание в развороте на первых двух
оборотах (de92063), это −5 с. Остальное вернул как было.

**Уроки, записанные кровью:**
* замер, меняющий тайминги, — это правка, а не замер;
* прежде чем откатывать по признаку, проверить, что признак означает поломку.
  Здесь он означал другой маршрут к тому же месту;
* паузы 0.4 после колеса/наклона и 1.2 после респавна — не запас, а время, за
  которое встают камера и персонаж.

**Заодно починено по дороге:**
* «ДВЕРЬ ОТКРЫТА N с» больше не врёт: замок читает счётчик ДО респавна. Кадр
  03:08:37 — за диалогом респавна видны «You locked your base for 90 Seconds!» и
  «Locked: 86s», то есть база была заперта, а бот насчитал 142 с открытой двери;
* клавиши отпускаются на выходе процесса (atexit). Жёсткая остановка оставляла
  зажатой «w», и следующий бот получал персонажа, который сам идёт вперёд;
* один хозяин ввода на машину — `brainbot/single.py`, теперь и для тестовых
  скриптов, а не только для фермы;
* `scripts/night.sh` — ночной надзор до N ребёрнов: поднимает клиент, если окно
  умерло, перезапускает круг, запирает дверь после каждого прогона.

---

## Опыт 03.09: малолюдный сервер — НЕ решение воровства

Замысел выглядел верным: всё, что стоит на базе, уносят живые соседи, а
публичных серверов сотни, среди них есть с одним игроком из восьми
(`launcher.quietest_job_id`, HTTP 200, сто серверов в ответе).

Чем кончилось за сорок минут:

* заход в сервер с ОДНИМ игроком дважды подвесил клиент на «Joining server»
  дольше двух минут — почти пустой сервер уже закрывается, попасть некуда;
* на чужой базе перестала работать дорога к плите: вид сверху пад находит
  (пеленг +106), а свечения «не видно даже осмотром». Лок не встал НИ РАЗУ за
  полчаса, дверь стояла открытой по 300 с — то есть база была беззащитнее, чем
  на людном сервере;
* воровство осталось: кадр 10:55 на сервере с ДВУМЯ игроками — прямым текстом
  «Someone is stealing your Avocadini Guffo».

Откат: сервер снова выбирает игра (02d8741), локи вернулись (44 и 28 с).
`quietest_job_id` оставлен в коде, но включать его — только вместе с починкой
дороги к плите на произвольной базе.

**Что из этого следует для воровства.** Тихий сервер отпадает, остаётся
единственный рычаг — держать дверь закрытой плотнее. Сейчас это 17-18 с за
круг, и в эти окна нас обносят. Дальше резать некуда: 17 с — стоимость самого
ритуала (респавн 5.5 + камера 7.3 + подход). Значит либо ускорять ритуал, либо
менять сам круг так, чтобы база открывалась реже.
