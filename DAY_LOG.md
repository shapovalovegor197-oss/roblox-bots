# Журнал 30.08.2026 (день)

Работаю по пунктам плана, каждый — до результата числами.

## Пункт 1. Скорость лока — ЗАКРЫТ

Было (ночь, 07:55): 80–85 с при надёжности 5/5.
Стало: **4 из 4, время 15.8 / 5.7 / 15.5 / 5.5 с, среднее 10.6 с.**
Цель была ≤25 с.

Что решило — не ускорение ходьбы, а **чтение подтверждения**:

* подтверждение лока ловится вспышкой «You locked your base for 80 Seconds!»
  ВНИЗУ кадра (y≈0.87, x≈0.50), а не счётчиком «Locked: Ns» у входа. Счётчик
  висит в мире и пропадает, стоит камере отвернуться, — поэтому бот не видел
  собственного успеха и сжигал вторую попытку (+11 с) на каждом локе;
* длительность лока игра говорит ОДИН РАЗ, вспышкой. Дальше считаем секунды
  сами (`lock_until`), а не перечитываем счётчик: зрение в этом месте больше
  не участвует;
* прежний стенд `lock_e2e.py` врал. `read_lock_left` изредка не читает счётчик,
  стенд решал «лок снят» и шёл запирать, а `lock_via_top` видел живой таймер и
  возвращал его же за 14 с. Три «успешных прогона» подряд оказались ОДНИМ
  локом. Новый стенд `lock_bench.py` проверяет снятие тремя независимыми
  признаками (свои часы, счётчик, свечение плиты).

Фазы удачного подхода: таблица 0.3, разворот 5.2, наведение 0.5, подход 2.0,
дошаг 0.7–1.8. Самая дорогая — разворот сверху (5.2 с), это следующий резерв.

Длительность лока подтверждена вспышкой: **80 секунд**.

## Пункт 2. Круг целиком — ЗАКРЫТ

Лок 16.8 с → лента 16.1 с → бюджет 55 с → **2 покупки** ($164 360 → $134 360) →
домой. Дальше круг гоняется автономно (`scripts/farm_loop.py`).

## Пункт 3. Требования перерождения — ЗАКРЫТ

Читаются с экрана: **$12.5M**, **Trulimero Trulicina** и **Chimpanzini
Bananini**, оба тёмными силуэтами (насыщенность иконки 17.4 — «не куплен»).

Почему не читалось раньше, две причины:

* OCR русской локали подставляет кириллицу в ЧИСЛА: «$ 134.зк $ 12.5м». Плюс
  теряет слэш. Разбор требовал `/` и латиницы — и возвращал None, то есть ни
  сумма, ни предметы не проверялись вовсе;
* подписи под иконками мелкие, полный кадр их не берёт. Коробки требований
  ищутся контурами (тёмные квадраты на светлой панели), подпись читается
  бинаризацией порогом 160 с увеличением ×6. Скользящее окно по всему тексту
  окна не годится: из обрывков «ва nini» рождался несуществующий «Tenini
  Ballini».

## Три молчаливых поломки, найденные по ходу

**1. Незакрытое окно REBIRTH делало бота слепым.** OCR читает заголовок как
`nebirth`, в списке окон стояло `rebirth` — совпадения нет, `dismiss_modals`
отвечал «экран чист», ничего не закрыв. Окно висело в середине кадра, то есть
ровно там, где живут вид сверху и свечение плиты. Четыре лока подряд провалены,
в логе честное «свечения не видно даже осмотром». Теперь признак окна — САМ
КРЕСТИК (по цвету и форме), а не заголовок; после клика закрытие проверяется.

**2. Чтение наличных врало в РАЗЫ.** Вырез захватывал иконку слотов и строку
Friend Boost, OCR склеивал их с суммой: $81.86K приходил как 681 860 и даже
48 186 000. На таких числах «подтверждались» покупки. Теперь вырез ищется по
самой зелёной надписи, и обязателен знак `$` — его потеря и давала «381.86k».
Проверка: пять чтений подряд дали одно и то же значение.

**3. Наведение на плиту перелетало цель.** Перевод «пиксели → единицы мыши»
верен только для той дистанции, на которой его мерили. Знак промаха скакал
(−0.218 → −0.190 → +0.211), наведение стоило 18.8 с. Теперь масштаб доворота
подстраивается на ходу: сменил знак и не уменьшился — шаг вдвое короче.
Наведение стало 0.5–1.3 с.

Лок после всего этого: **4 из 4, 5.7–19.4 с (среднее 14.7)** — уже на непустой
базе и при сменившемся оформлении.

## ГЛАВНОЕ за день: деньги надо СОБИРАТЬ

Ночная запись «доход капает сам, собирать не надо» — **неверна**. Над каждым
брейнротом висит подпись `Collect $N`, и сумма забирается ПРОХОДОМ мимо. На
экране одновременно висели `Collect $719.1K`, `$155.9K`, `$116.5K`, `$12.2K`,
а на руках было $81.86K. Замер: проход в семь шагов вглубь базы дал
**$81 860 → $1 180 000**, подписи из кадра исчезли.

Прежний `collect_money` жал `E` на паде и обходил окрестность — он не мог
собрать ничего, потому что нажатие тут ни при чём.

Отсюда же объяснение «нулевого дохода»: деньги копились на брейнротах, а HUD
стоял на месте, и я почти счёл базу пустой.

**И вторая половина той же картины: нас обкрадывают.** На кадре прямым текстом
«Someone is stealing your Boneca Ambalabu». То есть незапертая база — это не
теоретический риск, а то, что происходит прямо сейчас, и лок надо держать
максимально плотно.

## Как именно собираются деньги (выяснено замерами)

У КАЖДОГО брейнрота свой зелёный пад с подписью `Collect $627.9K` — деньги
берутся, когда **наступаешь на этот пад**, а не проходом по центру базы.
Отсюда все неудачи: между двумя рядами можно пройти базу насквозь, никого не
задев. Замер провального прохода: три круга подряд «собрано 0» при $694 800 и
$571 800, висевших в кадре.

Удачные проходы: **$81 860 → $1 180 000**, **$846 760 → $1 550 000**,
**$723 760 → $7 380 000**.

Рядом с брейнротом видны ещё два промпта: `E — Grab` (взять брейнрота) и
`F — Sell: $12.5K` (продать). Это пригодится для передачи складу и для
освобождения слотов.

**Я сам себе мешал.** Проверка «рост правдоподобен» стояла «не больше чем в
десять раз», а сбор увеличил деньги в девять с половиной — и настоящий сбор на
семь миллионов трижды подряд объявлялся неудачей. Граница теперь только
абсолютная.

## Пункт 4. Автономный цикл — РАБОТАЕТ

`scripts/farm_loop.py`: цели из окна ребёрна (или из памяти), лок → сбор →
лента → закуп по окупаемости → домой. Пишет `var/farm_status.json`.

Числа последнего прогона: лок 49.8 с, сбор $660 000, потом лок 49.9 с, сбор
$380 000. Деньги: **$8.42M из нужных $12.5M**.

## Итог дня (к 16:50)

**Деньги: $81 860 утром → $18 010 000.** Порог перерождения ($12.5M) пройден с
запасом. Взято это почти целиком сбором, а не покупками.

Что работает без присмотра, числами последних прогонов:

* **лок** — 16.7 / 18.4 / 18.8 / 19.1 / 22.3 с, провалов в этой серии нет
  (утром было 80–85 с при 5 из 5);
* **сбор** — поперечный проход по рядам падов, за круг приносит $200 000 –
  $2 760 000;
* **закуп** — по окупаемости: берём, если цена окупается быстрее 400 секунд.
  Куплены Sigma Boy ($1350/с), Orangutini Ananassini ($3100/с),
  Frigo Camelo ($2000/с) и мелочь;
* **цикл целиком** гоняется сам: `python scripts/farm_loop.py <минут> <мин_доход>`,
  состояние в `var/farm_status.json`.

**Что НЕ сделано и почему:**

* **перерождение не делал и не буду без тебя.** Деньги набраны, но требуются ещё
  два именных брейнрота — **Trulimero Trulicina** и **Chimpanzini Bananini**, —
  а их на ленте пока не выпало (бот караулит их адресно, покупает вне очереди).
  И главное: панель прямо предупреждает, что ребёрн СОТРЁТ все деньги и всех
  брейнротов. Это твоё решение, не моё;
* **сбор стоит 40–45 с из 80-секундного окна лока**, поэтому у ленты остаётся
  мало времени. Сейчас собираю через круг — компромисс, а не решение. Правильно
  бы: собирать точечно, наводясь на ближайший зелёный пад, а не обходить ряды
  целиком;
* **чтение наличных всё ещё врёт** в отдельных кадрах (даёт то 71 млн, то 7).
  Фильтр правдоподобия ловит выбросы, но это подпорка. Надёжнее читать сумму из
  панели ребёрна — там она нарисована на ровном фоне;
* **нас обкрадывают.** Пока база открыта (20 секунд между локами), соседи
  уносят брейнротов; один раз я видел это прямым текстом на экране.
